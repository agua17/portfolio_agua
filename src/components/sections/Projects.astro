---
import { Image } from "astro:assets";
import Section from "../Section.astro";
import {basics} from "@cv"
import fisio from "../../assets/fisioterapeuta.png"

const {name, label, location,profiles,email,phone} = basics

---

<Section title="Proyectos">
    

<div class="max-w-sm bg-white border border-gray-200 rounded-lg shadow dark:bg-gray-800 dark:border-gray-700">
    <figure class="rounded-t-lg">
        <Image src={fisio} alt={name} />
    </figure>
    <div class="p-5">
        <a href="#">
            <h5 class="mb-2 text-2xl font-bold tracking-tight text-gray-900 dark:text-white">Fisioterapia Web</h5>
        </a>
        <p class="mb-3 font-normal text-gray-700 dark:text-gray-400">Desde este proyecto mostramos los beneficios de diferentes tipos de masajes que ofrece una clínica de Sevilla.</p>
        <!-- Botón para abrir el modal -->
        <button
            id="openModalBtn"
            class="inline-flex items-center px-3 py-2 text-sm font-medium text-center text-white bg-blue-700 rounded-lg hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800"
        >
            Acceder
            <svg class="rtl:rotate-180 w-3.5 h-3.5 ms-2" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 14 10">
                <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M1 5h12m0 0L9 1m4 4L9 9"/>
            </svg>
        </button>
    </div>
</div>

<!-- Modal -->
<div
    id="modal"
    class="hidden fixed inset-0 z-50 bg-black bg-opacity-50 flex justify-center items-center"
>
    <div class="bg-white p-4 rounded-lg">
        <!-- Imagen del modal desde la carpeta assets -->
        <img
            src="/src/assets/actualizando_web.png"  
            alt="Imagen Modal"
            class="max-w-full"
        />
        <button
            id="closeModalBtn"
            class="mt-2 px-4 py-2 bg-red-500 text-white rounded-lg hover:bg-red-700"
        >
            Cerrar
        </button>
    </div>
</div>
</Section>



    
</Section>

